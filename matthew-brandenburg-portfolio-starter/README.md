# Matthew W. Brandenburg — Portfolio

This is a static personal website hosted on **GitHub Pages** and fronted by **Cloudflare**.

## Quick edits
- Edit `index.html` for content.
- Put images in `assets/` and `assets/gallery/`.
- Replace `assets/Matthew_Brandenburg_Resume.pdf` when updating your resume.
- GitHub Pages is enabled via repo settings. Custom domain is set with a `CNAME` file and Cloudflare DNS A/CNAME records.

## Custom domain
This repo includes a `CNAME` file with `matthew-brandenburg.com`. If you fork/rename, update or remove it.
